---
import ProjectDetails from "../components/storyblok/ProjectDetails.astro";
const { story } = Astro.props;
---

<article class="">
  <ProjectDetails
    blok={story.content.details[0]}
    storyTitle={story.name}
    altLayout={true}
    storyName={story.name}
  />
</article>
